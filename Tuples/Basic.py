myTuple = ("cherry", "apple", "banana")
#   The following is a tuple and is ordered and cannot be
#   Changed and tuple are indexed just like list therefore
#   tuple allow for duplicate values
newTuple = ("watermelon")   # Not a Tuple
#   if no , at end of a tuple element python doesn't recognise as tuple
newTuple1 = ("watermelon", )    # A tuple
if "apple" in myTuple:
    print("yes")